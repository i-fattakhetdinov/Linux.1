Bash-скрипт, который бэкапит все файлы с кодом, которые лежат не выше домашнего каталога текущего пользователя.

**Пример запуска:** 
    
    ./backup.sh -d directoryName -a archieveName -e "cpp ipynb c"
